import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    userItem: {
        paddingBottom: 16
    },
    heading: {
        fontWeight: 600,
        textAlign: 'center',
        fontSize: 18,
        marginBottom: 8
    },
    wrapper: {
        borderBottomWidth: 2
    }
})

export default styles